NODE_ENV=testing
PORT=5000
DB_USER=postgres
DB_PASSWORD=admin
DB_HOST=localhost
DB_PORT=5432
DB_NAME=matrix_delivery_test
DATABASE_URL='postgresql://postgres:admin@localhost:5432/matrix_delivery_test'
JWT_SECRET=super_long_secret_key_for_testing_purposes_must_be_at_least_64_chars
JWT_REFRESH_SECRET=super_long_refresh_secret_key_for_testing_purposes_must_be_at_least_64_chars
FRONTEND_URL=http://localhost:3000
STRIPE_SECRET_KEY=sk_test_placeholder
WITHDRAWAL_PIN_REQUIRED=false

# Firebase Admin SDK Service Account (JSON string, not file path)
# Generate from: Firebase Console → Project Settings → Service Accounts
FIREBASE_SERVICE_ACCOUNT_JSON='{"type": "service_account",...}'
