*** Begin Patch
*** Update File: backend/middleware/rateLimit.js
@@
-const rateLimit = require('express-rate-limit');
+const { rateLimit, ipKeyGenerator } = require('express-rate-limit');
@@
-    keyGenerator: (req) => {
-      // Support FingerprintJS or similar device fingerprinting
-      // Fallback to IP address if header is missing
-      return req.headers['x-device-fingerprint'] || req.ip;
-    },
+    keyGenerator: (req) => {
+      // Prefer a stable device fingerprint when present; otherwise use
+      // express-rate-limit's IPv6-safe helper to avoid bypass issues.
+      return req.headers['x-device-fingerprint'] || ipKeyGenerator(req);
+    },
@@
-  keyGenerator: (req) => {
-    // Use user ID if authenticated, otherwise fall back to IP/fingerprint
-    if (req.user && req.user.userId) {
-      return user:;
-    }
-    return req.headers['x-device-fingerprint'] || req.ip;
-  }
+  keyGenerator: (req) => {
+    // Use user ID if authenticated; otherwise IPv6-safe fallback.
+    if (req.user && req.user.userId) {
+      return user:;
+    }
+    return req.headers['x-device-fingerprint'] || ipKeyGenerator(req);
+  }
*** End Patch