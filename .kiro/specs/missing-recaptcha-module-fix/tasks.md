# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Fault Condition** - Server Startup Crash on Missing Module
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For this deterministic bug, scope the property to concrete failing cases: server startup with missing recaptcha module
  - Test that server starts successfully when RECAPTCHA_ENABLED='false' and recaptcha module exists
  - Test that authController.js loads without crashing
  - Test that authentication endpoints become available
  - The test assertions should match the Expected Behavior Properties from design (Property 1: Server Starts Successfully)
  - Run test on UNFIXED code (with missing backend/utils/recaptcha.js)
  - **EXPECTED OUTCOME**: Test FAILS with "Cannot find module '../utils/recaptcha'" (this is correct - it proves the bug exists)
  - Document counterexamples found: server crashes at module load time, endpoints unavailable
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Recaptcha Verification When Enabled
  - **IMPORTANT**: Follow observation-first methodology
  - **NOTE**: Since the server cannot start with unfixed code, create a temporary stub module to observe behavior
  - Create temporary backend/utils/recaptcha.js with basic Google reCAPTCHA verification
  - Observe behavior on TEMPORARY STUB for recaptcha verification when RECAPTCHA_ENABLED='true'
  - Observe: Valid tokens are accepted and return true
  - Observe: Invalid tokens are rejected and return false
  - Observe: Security events are logged for failed verifications with IP and category
  - Observe: RECAPTCHA_ENABLED flag is checked before verification attempts
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Property-based testing generates many test cases for stronger guarantees
  - Test that for all valid tokens, verification succeeds when enabled
  - Test that for all invalid tokens, verification fails when enabled
  - Test that security logging occurs for all failed verifications
  - Test that conditional check logic is preserved across all authentication endpoints
  - Run tests on TEMPORARY STUB
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Remove temporary stub after documenting behavior
  - Mark task complete when tests are written, run, and passing on temporary stub
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 3. Fix for missing recaptcha module

  - [x] 3.1 Create backend/utils/recaptcha.js module
    - Create the missing module file at backend/utils/recaptcha.js
    - Export verifyRecaptcha function that checks RECAPTCHA_ENABLED environment variable
    - When RECAPTCHA_ENABLED='false', return true immediately (no-op implementation)
    - When RECAPTCHA_ENABLED='true', perform actual Google reCAPTCHA verification via API
    - Use axios to POST to https://www.google.com/recaptcha/api/siteverify
    - Include secret key from RECAPTCHA_SECRET_KEY environment variable
    - Include the recaptcha token from the request
    - Parse and return the success field from the response
    - Add error handling for network failures and invalid responses
    - Add logging using backend/config/logger for verification attempts and results
    - Log security events for failed verifications with IP address and category
    - Validate RECAPTCHA_SECRET_KEY is present when RECAPTCHA_ENABLED='true'
    - _Bug_Condition: isBugCondition(input) where input.moduleBeingLoaded == 'authController.js' AND NOT fileExists('backend/utils/recaptcha.js')_
    - _Expected_Behavior: Server starts successfully regardless of RECAPTCHA_ENABLED setting (Property 1 from design)_
    - _Preservation: Recaptcha verification behavior when RECAPTCHA_ENABLED='true' must remain unchanged (Property 2 from design)_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2, 3.3, 3.4_

  - [x] 3.2 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Server Starts Successfully
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - Verify server starts successfully with RECAPTCHA_ENABLED='false'
    - Verify authController.js loads without errors
    - Verify authentication endpoints are available
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3_

  - [x] 3.3 Verify preservation tests still pass
    - **Property 2: Preservation** - Recaptcha Verification When Enabled
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - Verify valid tokens are still accepted when RECAPTCHA_ENABLED='true'
    - Verify invalid tokens are still rejected when RECAPTCHA_ENABLED='true'
    - Verify security logging still occurs for failed verifications
    - Verify conditional check logic is preserved across authentication endpoints
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 4. Checkpoint - Ensure all tests pass
  - Run all exploration and preservation tests
  - Verify server starts successfully with both RECAPTCHA_ENABLED='true' and 'false'
  - Verify all authentication endpoints work correctly
  - Verify recaptcha verification works when enabled
  - Verify no-op behavior works when disabled
  - Ask the user if questions arise
