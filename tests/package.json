{
  "name": "matrix-delivery-tests",
  "version": "1.0.0",
  "description": "E2E Tests for Matrix Delivery Application",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js",
    "test:smoke": "cucumber-js --tags @smoke",
    "test:location": "cucumber-js --tags '@location or @driver'",
    "test:translation": "node test-translations.js",
    "driver-location-tests": "cucumber-js --tags '@location or @driver'",
    "test:watch": "cucumber-js --watch",
    "report": "node utils/generate-report.js"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "playwright": "^1.40.1"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@playwright/test": "^1.40.1",
    "axios": "^1.13.2",
    "chai": "^6.2.1",
    "puppeteer": "^24.30.0"
  }
}
