{
  "name": "matrix-delivery-tests",
  "version": "1.0.0",
  "description": "E2E Tests for Matrix Delivery Application",
  "main": "index.js",
  "scripts": {
    "test": "set CUCUMBER_TEST=true&& cucumber-js",
    "test:smoke": "set CUCUMBER_TEST=true&& cucumber-js --tags @smoke",
    "test:location": "set CUCUMBER_TEST=true&& cucumber-js --tags @location",
    "driver-location-tests": "set CUCUMBER_TEST=true&& cucumber-js --tags '@location or @driver'",
    "test:watch": "set CUCUMBER_TEST=true&& cucumber-js --watch",
    "report": "node utils/generate-report.js"
  },
  "dependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "playwright": "^1.40.1",
    "chai": "^4.3.10",
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1"
  }
}
